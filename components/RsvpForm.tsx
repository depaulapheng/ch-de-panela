"use client"; import {useState} from "react";
export function RsvpForm(){const [f,setF]=useState({name:"",phone:"",attending:true,companions:0,companionNames:"",dietaryRestriction:"",message:""});const [status,setStatus]=useState<{ok:boolean,msg:string}|null>(null);const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus(null);const r=await fetch("/api/rsvp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(f)});const j=await r.json();setBusy(false);if(!r.ok)return setStatus({ok:false,msg:j.error||"Não foi possível enviar."});setStatus({ok:true,msg:f.attending?"Presença confirmada! 🎉":"Obrigado por nos avisar. Sentiremos sua falta 💛"})}return <form className="form card info-card" onSubmit={submit}><div className="field"><label>Nome *</label><input className="input" required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="field"><label>Telefone / WhatsApp</label><input className="input" inputMode="tel" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></div><fieldset style={{border:0,padding:0}}><legend style={{fontWeight:600,marginBottom:8}}>Você poderá comparecer?</legend><label className="checkline"><input type="radio" checked={f.attending} onChange={()=>setF({...f,attending:true})}/> Sim, estarei lá 💛</label><label className="checkline"><input type="radio" checked={!f.attending} onChange={()=>setF({...f,attending:false,companions:0})}/> Infelizmente não poderei</label></fieldset>{f.attending&&<><div className="field"><label>Quantidade de acompanhantes</label><input className="input" type="number" min="0" max="10" value={f.companions} onChange={e=>setF({...f,companions:Number(e.target.value)})}/></div><div className="field"><label>Nome dos acompanhantes</label><input className="input" value={f.companionNames} onChange={e=>setF({...f,companionNames:e.target.value})}/></div><div className="field"><label>Restrição alimentar</label><input className="input" value={f.dietaryRestriction} onChange={e=>setF({...f,dietaryRestriction:e.target.value})}/></div></>}<div className="field"><label>Mensagem para o casal</label><textarea className="textarea" value={f.message} onChange={e=>setF({...f,message:e.target.value})}/></div>{status&&<div className={`notice ${status.ok?"success":"error"}`}>{status.msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Enviando...":"Enviar resposta"}</button></form>}
